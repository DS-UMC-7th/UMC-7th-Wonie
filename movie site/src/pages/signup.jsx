import styled from 'styled-components';

const signup = () => {
    return (
        <StyledContent>회원가입 페이지</StyledContent>
    );
};

export default signup;

const StyledContent = styled.h1`
    color: white;
    margin-top: 5px;
`;