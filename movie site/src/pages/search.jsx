import styled from 'styled-components';

const search = () => {
    return (
        <StyledContent>검색 페이지</StyledContent>
    );
};

export default search;

const StyledContent = styled.h1`
    color: white;
    margin-top: 5px;
`;