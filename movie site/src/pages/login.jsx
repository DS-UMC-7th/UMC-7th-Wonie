import styled from 'styled-components';

const login = () => {
    return (
        <StyledContent>로그인 페이지</StyledContent>
    );
};

export default login;

const StyledContent = styled.h1`
    color: white;
    margin-top: 5px;
`;